{
  "name": "tutorial-test",
  "version": "1.0.0",
  "description": "",
  "main": "igniter.js",
  "scripts": {
    "dev": "lite-server",
    "build": "esbuild ./src/igniter.ts --bundle --minify --sourcemap --outfile=dist/igniter.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.21",
    "lite-server": "^2.6.1",
    "typescript": "^4.5.5"
  }
}
